const regex1= /bad/g
const text1 = " gaurav is bad bad very bad boy"
console.log(text1.replace(regex1, "good"))

const regex = /.arry/g
const text = " gaurav is bad bad very bad boy larry parry"
console.log(text.replace(regex, "good"))
